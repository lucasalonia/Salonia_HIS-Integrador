
doctype html
html
  head
    title= title
    link(rel='stylesheet', href= '/css/estiloIngreso.css')
  body
    h1 Bienvenido a HIS 
    div.validacion 
        h2 Ingreso al sistema 
        form(action='/ingreso', method='POST')
            div
                label(for='usuario') Usuario:
                input(type='text', name='usuario', required=true)
            div
                label(for='contraseña') Contraseña:
                input(type='contraseña', name='contraseña', required=true)
            div
                button(type='submit') Ingresar POST a /login

 
